# Radar Smart Contacts — Release Note

This production release adds explainable contact identity resolution, duplicate review, reversible merges, automatic potential with manual override, manual priority, favorites, and low-click Radar controls while preserving the existing production sales journey, saved message models, follow-up state, tenant boundaries, and owner-only personal vault behavior.
