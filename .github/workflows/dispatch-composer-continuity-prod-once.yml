name: Dispatch Composer Continuity Production Once

on:
  push:
    branches:
      - ops/release-composer-continuity-prod
    paths:
      - .github/workflows/dispatch-composer-continuity-prod-once.yml

permissions:
  contents: read
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch canonical production release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "/repos/${GITHUB_REPOSITORY}/actions/workflows/connect-core-production-release.yml/dispatches" \
            -f ref=production \
            -f 'inputs[live_mode]=true' \
            -f 'inputs[live_activation_ack]=CONNECT_LIVE_READY'
          echo "COMPOSER_CONTINUITY_PRODUCTION_RELEASE_DISPATCHED"
